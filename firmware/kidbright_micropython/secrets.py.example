# (ไม่บังคับ) คัดลอกเป็น secrets.py บนบอร์ดเพื่อทับค่าใน main.py — ไม่มีไฟล์นี้ก็รันได้

WIFI_SSID = "your_wifi_name"
WIFI_PASSWORD = "your_wifi_password"

MQTT_HOST = "iot.cpe.ku.ac.th"
MQTT_PORT = 1883
MQTT_TOPIC = "b6710545709/field_monitoring"

# ถ้า broker ไม่ต้องการ user/password ให้เป็น None ทั้งคู่
MQTT_USER = None
MQTT_PASS = None

DEVICE_ID = "kidbright_01"

# ความถี่ส่งข้อมูล (มิลลิวินาที)
PUBLISH_INTERVAL_MS = 10_000

# ขา ADC อ่านความชื้นดิน (เช่น 34 บน ESP32) — ไม่ใช้ให้ตั้งเป็น None
SOIL_ADC_PIN = 34
